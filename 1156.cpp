#include <cstdio>
#include <algorithm>
using namespace std;

int main(){
	int x=0, i=0, p;
	do{
	scanf("%d", &x);
	if(x != 42)
	printf("%d\n", x);
	else
	break;
	}while(x!=42);

	
	return 0;
}
